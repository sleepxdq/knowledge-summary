spring:
  datasource:
    url: jdbc:mysql://localhost:3306/buct?characterEncoding=UTF-8
    username: root
    password: xdq
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: update
      naming-strategy: org.hibernate.cfg.ImprovedNamingStrategy
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect